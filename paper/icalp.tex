\documentclass[a4paper,UKenglish,cleveref,autoref,thm-restate,anonymous]{lipics-v2021}

\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{tikz}
\usetikzlibrary{arrows.meta}
\usepackage{array}

\bibliographystyle{plainurl}

\title{Social Choice Theory in Semiring}

\titlerunning{Social Choice Theory in Semiring}

\author{First Author}{Institution, Country}{first@example.org}{}{}
%\author{Second Author}{Institution, Country}{second@example.org}{}{}
%\authorrunning{F. Author and S. Author}

%\Copyright{First Author and Second Author}

\ccsdesc[500]{Theory of computation~Algebraic semantics}
\ccsdesc[300]{Theory of computation~Logic and verification}
\ccsdesc[300]{Mathematics of computing~Graph algorithms}

\keywords{Semirings, Kleene star, algebraic path problem, bottleneck semiring,
social choice, interactive theorem proving}

\category{Track B: Automata, Logic, Semantics, and Theory of Programming}

\newcommand{\Mstar}{M^{\ast}}
\newcommand{\clo}[2]{#1^{\ast\langle #2\rangle}}
\newcommand{\rocq}{\textsc{Rocq}}
\newcommand{\Trop}{\mathbb{T}}
\newcommand{\Diam}{\mathbb{D}}
\newcommand{\Bool}{\mathbb{B}}

\begin{document}

\maketitle

\begin{abstract}


The Schulze method is a widely used single-winner election method rule based on \emph{beatpaths}. One alternative beats 
another when the strongest path from one to the other is stronger than the strongest 
path in the opposite direction. Since the strength of a path is determined by its weakest 
link, this can be expressed using the Kleene star of a matrix over the 
semiring \(\langle \mathbb{Z}^{\pm\infty}, \max, \min\rangle\). 
Schulze's guarantees, such as neutrality, monotonicity, pareto, etc.,  
are therefore theorems about that semiring.

We interpret those guarantees over an arbitrary semiring and ask: 
for each guarantee, which algebraic axioms its proof consumes. 
They sort into five levels. At
the bottom is neutrality, which holds over any semiring at all. The Pareto
condition and monotonicity are proved from \emph{boundedness} alone, that
$1 + a = 1$. The Smith criterion and Condorcet consistency are proved from \emph{boundedness} together with \emph{selectivity},
that $x+y$ is always $x$ or $y$, and each of them fails without selectivity.
The criteria that compose two beatpaths use in addition a
\emph{meet property}, that $a\cdot b$ is the greatest lower bound of $a$ and
$b$. Reversal symmetry, in its relation-level form, is the outlier: it needs
only \emph{commutativity}, over a plain semiring.


\end{abstract}

\section{Introduction}\label{sec:intro}
The Schulze method~\cite{schulze2011new} ranks alternatives by
\emph{beatpaths}. From a profile of ballots, one derives a matrix $M$ of
pairwise link strengths. A chain $a = c_0, c_1, \dots, c_k = b$ is worth its
weakest link, and alternative $a$ outranks $b$ when the best chain from $a$
to $b$ beats the best chain from $b$ to $a$. Here best means $\max$ and
worth means $\min$. The matrix of best chains is then exactly the Kleene
star
\[
  \Mstar \;=\; I \oplus M \oplus M^{2} \oplus \cdots
\]
of $M$ over the semiring $\langle \mathbb{Z}^{\pm\infty}, \max, \min\rangle$.
The method's output follows from the comparison
\[
  a \succ b \quad\text{iff}\quad \Mstar_{ba} < \Mstar_{ab}
\]
taken in that semiring's natural order.


Now change the arithmetic,  from $\langle \mathbb{Z}^{\pm\infty}, \max, \min\rangle$ to 
$\langle \mathbb{N}^{\infty}, \min, +\rangle$, and keep everything else. 
Let a chain be worth the
\emph{sum} of its links instead of the weakest, and let the best chain be the
shortest one. Then $\Mstar$ holds shortest distances, and $a \succ b$ says
that $a$ reaches $b$ more cheaply than $b$ reaches $a$. Only the two
operations have moved, and three alternatives are already enough to make the
method fail.

\begin{example}[One profile, two semirings]\label{ex:tropical}
Take three alternatives and three voters ranking them cyclically: one votes
$a > b > c$, one votes $b > c > a$, and one votes $c > a > b$. Pairwise, $a$
beats $b$ two votes to one, $b$ beats $c$ two to one, and $c$ beats $a$ two
to one, so the majority relation is a cycle and there is no Condorcet
winner. Every method built on pairwise comparisons must say something about
this profile.

Over $\langle \mathbb{Z}^{\pm\infty}, \max, \min\rangle$, let $M_{ij}$ be
the number of voters ranking $i$ above $j$, with rows and columns indexed
$a,b,c$ and the semiring zero on the diagonal:
\[
  M \;=\;
  \begin{pmatrix}
    -\infty & 2 & 1 \\
    1 & -\infty & 2 \\
    2 & 1 & -\infty
  \end{pmatrix},
  \qquad
  \Mstar \;=\;
  \begin{pmatrix}
    \infty & 2 & 2 \\
    2 & \infty & 2 \\
    2 & 2 & \infty
  \end{pmatrix}.
\]
The closure lifts every losing direction to the winning one. The direct link
from $b$ to $a$ is worth only $1$, but the detour $b \to c \to a$ is worth
$\min(2,2) = 2$, which matches the direct link from $a$ to $b$; the other
two pairs behave the same way. The cycle
in the ballots is flattened into indifference and all three alternatives
survive as winners.

Now keep the profile and change the semiring to $\langle \mathbb{N}^{\infty},
\min, +\rangle$, reading a link as a cost rather than a strength and
charging each link the opposition to it, $N_{ij} = M_{ji}$. Each hop along
$a \to b \to c \to a$ then costs $1$ and each reverse hop costs $2$:
\[
  N \;=\;
  \begin{pmatrix}
    \infty & 1 & 2 \\
    2 & \infty & 1 \\
    1 & 2 & \infty
  \end{pmatrix},
  \qquad
  N^{\ast} \;=\;
  \begin{pmatrix}
    0 & 1 & 2 \\
    2 & 0 & 1 \\
    1 & 2 & 0
  \end{pmatrix}.
\]
This time the closure changes nothing off the diagonal. The detour
$b \to c \to a$ costs $1 + 1 = 2$, no better than the direct hop back, and
nowhere near the direct hop from $a$ to $b$ at cost $1$. So $a \succ b$, and
by symmetry $b \succ c$ and $c \succ a$: the cycle survives the closure as a
cycle of \emph{strict} wins, every alternative is beaten by another, and the
winner set is empty.
\end{example}

The two halves of \Cref{ex:tropical} run the same ballots through the same
construction and disagree completely. The guarantee that Schulze's
method returns an ordering with a winner therefore does not come from the
beatpath idea alone; it comes from $\max$ and $\min$. This is one 
guarantee and Schulze's method has many
others. Raising a candidate's support never harms it (monotonicity). A
candidate who beats every other head to head is always elected (Condorcet
consistency). Every winner comes from the smallest set of candidates that
beats everyone outside it (the Smith criterion). And there are several more.


It has long been known that the closure construction itself is generic:
transitive closure, shortest paths, and widest paths are all instances of
one algorithm run over different
semirings~\cite{carre1971algebra,lehmann1977,10.5555/1386688,baras2022path}.
To our knowledge, though, social choice theory has never been carried to
that same level of generality. That is our subject. For each of Schulze's
guarantees we ask the same question: which algebraic axioms does its proof
actually need?

\subparagraph*{Our contributions.}
Interpreting the social choice theory over a semiring
turns each of Schulze's criteria into a statement about that semiring. Each
such statement then survives or fails depending on the axioms the carrier
satisfies, so the criteria stop being a flat list and acquire a structure.
Describing that structure takes four definitions: (i) \emph{idempotence},
(ii) \emph{boundedness}, (iii) \emph{selectivity}, and (iv) the \emph{meet
property}. Our contributions are the following.

\begin{enumerate}
\item \textbf{A classification of Schulze's criteria by algebraic strength}
(\Cref{sec:classification}). We prove each criterion at the weakest level of
structure at which we can establish it, and those levels form a four-step
hierarchy and each level adds one axiom to the previous one. 
\begin{itemize}
\item \textbf{Level 0, no assumptions.} Neutrality holds for every
semiring.
\item \textbf{Level 1, boundedness.} Weak Pareto and monotonicity.
\item \textbf{Level 2, boundedness and selectivity.} The Smith criterion,
Smith-IIA (Smith independence of irrelevant alternatives), Condorcet consistency, 
and the resolvability proof.
\item \textbf{Level 3, boundedness, selectivity, and the meet property.}
Transitivity of $\succ$, existence of a winner, independence of clones,
the winner-level form of reversal symmetry, prudence, and the MinMax set.
\end{itemize}
\end{enumerate}

\section{Background}\label{sec:prelim}
\subsection{Semirings and the Natural Order}

\begin{definition}[Semiring]
A \emph{semiring} is a tuple $R = \langle R, +, \cdot\,, 0, 1\rangle$ where
$\langle R,+,0\rangle$ is a commutative monoid, $\langle R,\cdot\,,1\rangle$ is
a monoid, multiplication distributes over addition on both sides, and
$0\cdot a = a \cdot 0 = 0$. It is \emph{idempotent} if $a+a=a$,
\emph{commutative} if $\cdot$ is, and \emph{bounded} if $1 + a = 1$ for all
$a$.
\end{definition}

Boundedness says $1$ absorbs $+$; it implies idempotence and is the standing
assumption throughout. In the intended reading it is not a restriction but a
fact: the empty path should be unbeatable.

\begin{definition}[Natural order]
For an idempotent semiring put $a \leq b \iff a+b=b$, and write $a<b$ for
$a\leq b$ with $a \neq b$.
\end{definition}

\begin{lemma}\label{lem:natural}
On an idempotent semiring the natural order is a partial order, $+$ is its
join, and multiplication is monotone in both arguments. If $R$ is bounded then
$0$ is least, $1$ is greatest, and $a\cdot b \leq a$ and $a\cdot b \leq b$.
\end{lemma}

\begin{proof}
Reflexivity is the idempotence axiom itself, since $a \leq a$ unfolds to
$a + a = a$. The proofs of transitivity, antisymmetry, and monotonicity are
each a one-line rewrite; for transitivity, $a+b=b$ and $b+c=c$ give
$a + c = a + (b + c) = (a+b) + c = b + c = c$, and the other two are
similar. For the bounded clauses, $0+a=a$ and $1+a=1$ give the extremes,
and $a\cdot b + a = a(b+1) = a$ gives $a\cdot b \leq a$, symmetrically on
the other side.
\end{proof}

The last clause separates this setting from arbitrary semirings: here
multiplication only loses strength. That is the right reading when a product
is the worth of a composite chain, and it fails in
$\langle \mathbb{N}, +, \times\rangle$, which is therefore out of scope.

\begin{definition}[Selectivity, meet property]\label{def:sel-meet}
$R$ is \emph{selective} if $x+y \in \{x,y\}$ for all $x,y$. It has the
\emph{meet property} if $m \leq a$ and $m \leq b$ imply $m \leq a\cdot b$.
\end{definition}

Selectivity is exactly totality of the natural order, and with
\Cref{lem:natural} the meet property says $a\cdot b$ is the greatest lower
bound of $\{a,b\}$.

\subsection{Matrices and the Kleene Star}

\begin{definition}[Matrices and the star]
Fix a finite set $A$ of alternatives, $|A| = n$. A \emph{matrix} over $R$
assigns to every ordered pair of alternatives a strength, $M : A \times A
\to R$, written $M_{ij}$. Matrices form a semiring under entrywise
addition $(M\oplus N)_{ij} = M_{ij}+N_{ij}$ and the usual product
$(M\otimes N)_{ij} = \sum_k M_{ik}\cdot N_{kj}$; the identity $I$ carries
$1$ on the diagonal and $0$ elsewhere. The \emph{star} of $M$ is the
series
\[
  \Mstar
  \;=\; \bigoplus_{ 0 \leq k} M^{k}
  \;=\; I \oplus M \oplus M^{2} \oplus \cdots .
\]
\end{definition}

Entrywise, $(M^{k})_{ij}$ joins the worths of all walks of length exactly
$k$ from $i$ to $j$, so $\Mstar_{ij}$ joins the worths of all walks from
$i$ to $j$, of every length. In a general semiring this join is an
infinite sum and need not exist; over $\langle \mathbb{N}, +,
\times\rangle$ the powers simply grow.
Boundedness is what brings the series to a fixed point. Over a bounded
semiring, a walk of length $n$ or more revisits some vertex, and cutting
out the cycle between the two visits leaves a shorter walk of at least the
same worth, because products only lose strength (\Cref{lem:natural}).
Every power beyond $M^{n-1}$ is therefore dominated by the earlier ones
and adds nothing to the join. The series stabilises at stage $n-1$,
\[
  \Mstar \;=\; I \oplus M \oplus M^{2} \oplus \cdots \oplus M^{n-1}
  \;=\; (M \oplus I)^{n-1},
\]
where the second equality uses idempotence: expanding $(M \oplus I)^{n-1}$
by distributivity produces each $M^{k}$ many times over, and $a + a = a$
merges the duplicates. The development takes this truncated sum as the
definition of the closure. That is not a compromise but the
standard definition in the algebraic path literature, where the closure
over a suitably stable semiring is obtained at a finite
power~\cite{carre1971algebra,lehmann1977}. The name is not merely conventional:
the truncation satisfies $\Mstar = I \oplus (M \otimes \Mstar)$, the
defining equation of the Kleene star for matrices, 
and \Cref{sec:walks} shows it computes
exactly the simple-path optimum the method prescribes. The truncation
remains meaningful even without boundedness, and the one bare-semiring
result, neutrality, is a statement about it.

\section{Beatpath over a Semiring}\label{sec:framework}

\begin{definition}\label{def:succ}
For a matrix $M$ over an idempotent semiring put
\[
  a \succ_M b \quad\text{iff}\quad \Mstar_{ba} < \Mstar_{ab},
\]
dropping the subscript when $M$ is clear, and write $\mathcal{S}(M)$ for the
set of $\succ$-maximal alternatives, the \emph{winners}.
\end{definition}

The comparison in \Cref{def:succ}, in fact, makes sense over any matrix, and reading
it on the direct matrix $M$ instead of on the closure gives another 
relation: $a$ beats $b$ \emph{head to head} when $M_{ba} < M_{ab}$, that is,
when the direct link from $a$ to $b$ is strictly stronger than the direct
link back. This is the Condorcet relation, and an alternative that beats
every other head to head is a \emph{Condorcet winner}. The two relations
can disagree, because the closure lets $a$ outrank $b$ through
intermediaries even when $b$ wins the direct comparison. 
Condorcet consistency is the
criterion that connects them: a Condorcet winner, already unbeaten in the
direct relation $M$, must remain a winner in closure $\Mstar$.

Our development takes $M$ as a primitive. The Schulze method derives $M$
from a profile of ballots, but the criteria below are stated and proved at
the matrix level, over an arbitrary semiring. That is where the algebra
lives, but it is not where all of Schulze's arguments live: Pareto appeals
to the transitivity of each ballot, Smith and Condorcet to the fact that
$M_{ab}$ and $M_{ba}$ are read off one pair of counts, and monotonicity and
independence of clones to how those counts move when ballots change. A
matrix has discarded all of this, so each such criterion carries the fact
its proof needs as an explicit hypothesis on $M$, and
\Cref{tab:classification} marks the ones that do.

A reader may reasonably ask whether those hypotheses are doing too much of
the work. They are not, and a second layer shows why: it derives them
instead of assuming them. That layer builds $M$ from ballots. A ballot ranks
the alternatives, a profile is a list of ballots, and the entry $M_{ab}$
comes from the number of voters who prefer $a$ to $b$ and the number who
prefer $b$ to $a$. Any way of turning that pair of counts into a strength
will do, provided it meets two conditions of Schulze's: more support with no
more opposition makes a link stronger, and a win counts for more than a tie,
a tie for more than a loss. Winning votes, margins, and losing votes all
qualify. For each criterion the layer derives its hypotheses on $M$ from the
premise Schulze states about ballots, so the results apply to actual
elections and not only to abstract matrices.



\section{Which Axiom Does Each Criterion Need?}\label{sec:classification}

We now go through the criteria and record, for each, the weakest algebraic
hypothesis under which we can establish it. Throughout the section, $M$ and
$M'$ denote matrices over the carrier of the level in question, $a$, $b$,
$x$, and $y$ denote alternatives. \Cref{tab:classification}
summarises; the last column marks criteria that additionally carry a 
condition on the profile.

\begin{table}[t]
\centering
\begin{tabular}{@{}cllc@{}}
\toprule
Level & Criterion & Algebraic requirement & Profile \\
\midrule
0 & Neutrality                 & none, any semiring       & \\
\midrule
1 & Pareto, weak               & bounded                  & $\checkmark$ \\
1 & Monotonicity               & bounded                  & \\
\midrule
2 & Smith criterion            & bounded, selective       & $\checkmark$ \\
2 & Smith-IIA, weak side       & bounded, selective       & $\checkmark$ \\
2 & Smith-IIA, strong side     & bounded, selective       & $\checkmark$ \\
2 & Condorcet consistency      & bounded, selective       & $\checkmark$ \\
2 & Resolution step            & bounded, selective       & $\checkmark$ \\
\midrule
3 & Transitivity of $\succ$    & bounded, selective, meet & \\
3 & Winner existence           & bounded, selective, meet & \\
3 & Independence of clones     & bounded, selective, meet & \\
3 & Reversal symmetry          & bounded, selective, meet & \\
3 & Prudence                   & bounded, selective, meet & $\checkmark$ \\
3 & MinMax set                 & bounded, selective, meet & $\checkmark$ \\
\bottomrule
\end{tabular}
\caption{The hierarchy. Each row gives the weakest structure under which we
can establish the criterion. Levels 0 to 3 stack, each adding to the one
below.}
\label{tab:classification}
\end{table}


\subsection{Level 0: Any Semiring}

One criterion needs no algebraic assumption whatsoever.

\begin{theorem}[Neutrality]\label{thm:neutrality}
Let $\sigma$ be a permutation of $A$ and put
$M^{\sigma}_{ab} = M_{\sigma a\,\sigma b}$. Then $a \succ_{M^{\sigma}} b$ if
and only if $\sigma a \succ_{M} \sigma b$, and $a \in \mathcal{S}(M^{\sigma})$
if and only if $\sigma a \in \mathcal{S}(M)$.
\end{theorem}

Neutrality holds over any semiring at all. It reduces to the invariance of a
finite sum under a permutation of the index set, and the closure then
respects that permutation term by term. It needs neither boundedness nor
idempotence, and it is the only criterion we prove at the bare semiring
level.

\subsection{Level 1: Boundedness}

\subsection{Level 2: Selectivity}

\subsection{Level 3: The Meet Property}


\section{Formal Verification}

Our development is carried out in \rocq{}, and its proofs are constructive,
so the definitions extract to executable OCaml code that meets the
specifications proved of them. It runs to some $16{,}000$ lines across $37$
files. Nothing is admitted and no axiom is assumed.

Although our aim is to determine which semiring axioms each of Schulze's
criteria needs, the formalisation is not confined to that question.
Everything is parameterised over an arbitrary semiring, so instantiating the
carrier yields a verified implementation of any problem the same
construction solves. Besides the Schulze instance we extract and run three
others: shortest paths over $\langle \mathbb{N}^{\infty}, \min, +\rangle$,
widest-shortest paths, which minimise length and break ties by bottleneck
width, and a product semiring pairing latency with bandwidth that models
routing across $5$G network slices.

\section{Related Work}\label{sec:related}
\subparagraph*{Algebraic path problems.}

\subparagraph*{Algebraic conditions as the price of a guarantee.}

\subparagraph*{Mechanised algebra and verified social choice.}
Kleene algebra has been mechanised as a decision
procedure~\cite{10.1007/978-3-642-14052-5_13,MOREIRA2015377}, and functional
presentations of closure over semirings are
known~\cite{10.1007/978-3-319-17822-6_14}. Our development differs in how it
is organised. Because our claims concern minimality of hypotheses, each
theorem is stated at the weakest level of the hierarchy that supports it. On the
social choice side, formalisations of impossibility
theorems~\cite{arrow1950difficulty,nipkow2009social} and verified vote counting, including for
the Schulze method, are well
established~\cite{10.1007/978-3-319-66107-0_26,ghale2018modular,10.1007/978-3-030-03592-1_5}.
That work verifies a particular procedure at a fixed carrier. Our concern is
complementary: it asks how much algebraic structure a property costs.

\section{Conclusion}\label{sec:conclusion}


\bibliography{ref,icalp}

\end{document}
